syntax = "proto3";
package webRTC;

message Message {
  oneof type {
    string offer = 1;
    string answer = 2;
    Candidate candidate = 3;
  }
}

message Candidate {
  string candidate = 1;
  string sdpMid = 2;
  uint32 sdpMLineIndex = 3;
}
