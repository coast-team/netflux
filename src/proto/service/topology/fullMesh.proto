syntax = "proto3";
package fullMesh;

message Message {
  oneof type {
    Peers members = 1;
    bool requestMembers = 2;
    Peers intermediaryIds = 3;
    bool heartbeat = 5;
  }
}

message Peers {
  repeated uint32 ids = 1;
}
